---
const today = new Date().toLocaleDateString('es-MX', { 
    weekday: 'long', 
    year: 'numeric', 
    month: 'long', 
    day: 'numeric' 
});
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- VITAL PARA RESPONSIVE -->
    <title>Frank Lovelace Blog</title>
     <link rel="icon" type="image/png" href="/images/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700;900&family=Playfair+Display:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    
    <style is:global>
        /* Reseteo básico */
        *, *::before, *::after { box-sizing: border-box; }
        body { margin: 0; padding: 0; }
    </style>

    <style>
        body {
            background-color: #f4f1ea;
            color: #1a1a1a;
            font-family: 'Playfair Display', serif;
            overflow-x: hidden; /* Evita scroll lateral indeseado */
        }
        
        .newspaper-header {
            text-align: center;
            padding: 20px 10px;
            border-bottom: 3px double #1a1a1a;
            margin-bottom: 40px;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .meta-bar {
            display: flex;
            justify-content: space-between;
            border-bottom: 1px solid #1a1a1a;
            padding-bottom: 5px;
            margin-bottom: 15px;
            font-family: 'Cinzel', serif;
            font-size: 0.7rem;
            text-transform: uppercase;
        }

        h1.masthead {
            font-family: 'Cinzel', serif;
            font-size: clamp(2.5rem, 6vw, 5rem); /* Título responsivo: crece con la pantalla */
            font-weight: 900;
            line-height: 1;
            margin: 10px 0;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .nav-bar {
            border-top: 1px solid #1a1a1a;
            border-bottom: 1px solid #1a1a1a;
            padding: 10px 0;
            margin-top: 20px;
            display: flex;
            justify-content: center;
            flex-wrap: wrap; /* Permite que los links bajen en móvil */
            gap: 20px;
            font-family: 'Cinzel', serif;
            font-weight: bold;
            font-size: 0.9rem;
        }

        a { text-decoration: none; color: inherit; }
        a:hover { text-decoration: underline; }

        /* EL CONTENEDOR PRINCIPAL: AQUÍ ESTABA EL ERROR ANTES */
        .paper-container {
            max-width: 1200px; /* Ancho máximo para pantallas grandes */
            margin: 0 auto;
            padding: 0 20px 40px 20px;
        }
    </style>
</head>
<body>

    <header class="newspaper-header">
        <div class="meta-bar">
            <span>Vol. I — No. 1</span>
            <span>{today}</span>
            <span>Precio: 5 Knuts</span>
        </div>

        <h1 class="masthead">Frank Lovelace</h1>
        <p style="font-style: italic; margin-top: -5px; font-size: 0.9rem;">"We are shaped by our material conditions and social context, the rest is luck and a pinch of sugar"</p>

        <nav class="nav-bar">
            <a href="/">PORTADA</a>
            <a href="https://franklovelace.dev">MI PORTFOLIO (DEV)</a>
            <a href="https://art.franklovelace.dev">MI LADO ARTÍSTICO</a>
            <a href="https://instagram.com/franklovegood">INSTAGRAM</a>
        </nav>
    </header>

    <main class="paper-container">
        <slot /> 
    </main>

</body>
</html>